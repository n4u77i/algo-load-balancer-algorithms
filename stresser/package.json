{
  "name": "stresser",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "dev": "nodemon -r dotenv/config -e ts -x ts-node --transpile-only src/index.ts",
    "prod": "npx tsc && nodemon -r dotenv/config -e js build/index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@types/axios": "^0.14.4",
    "axios": "^1.7.8",
    "dotenv": "^16.4.5"
  }
}
